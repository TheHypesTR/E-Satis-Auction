/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect, useState } from 'react';
import { Check, Gavel, Plus, Search, X } from 'lucide-react';
import { adminApi } from '../services/adminApi';
import { auctionStatusLabel, formatMoney, getApiErrorMessage } from '../services/apiUtils';
import type { AdminProductListingSummaryDto } from '../types/admin';
import type { AuctionSummaryDto } from '../types/commerce';

function localIso(offsetMinutes: number) { return new Date(Date.now() + offsetMinutes * 60000).toISOString(); }

export default function AdminAuctions() {
  const [auctions,setAuctions]=useState<AuctionSummaryDto[]>([]); const [listings,setListings]=useState<AdminProductListingSummaryDto[]>([]); const [loading,setLoading]=useState(true); const [saving,setSaving]=useState(false); const [error,setError]=useState(''); const [message,setMessage]=useState(''); const [search,setSearch]=useState(''); const [showAdd,setShowAdd]=useState(false);
  const [form,setForm]=useState({productListingId:'',startingPrice:'',minimumBidIncrement:'10',startsAt:localIso(10),endsAt:localIso(70),quantity:'1',currency:'TRY'});
  const load=()=>{setLoading(true);Promise.all([adminApi.auctions({pageSize:100}),adminApi.listings({pageSize:100})]).then(([a,l])=>{setAuctions(a);setListings(l);}).catch(e=>setError(getApiErrorMessage(e,'Açık artırmalar yüklenemedi.'))).finally(()=>setLoading(false));}; useEffect(load,[]);
  const filtered=auctions.filter(a=>a.productName.toLowerCase().includes(search.toLowerCase())||a.sku.toLowerCase().includes(search.toLowerCase()));
  const create=async()=>{if(!form.productListingId||!form.startingPrice)return;setSaving(true);try{await adminApi.createAuction({productListingId:form.productListingId,sellerUserId:null,startingPrice:Number(form.startingPrice),minimumBidIncrement:Number(form.minimumBidIncrement),startsAt:form.startsAt,endsAt:form.endsAt,quantity:Number(form.quantity),currency:form.currency});setMessage('Auction oluşturuldu. Schedule/activate akışında stok rezervasyonu backend tarafından yönetilir.');setShowAdd(false);load();}catch(e){setError(getApiErrorMessage(e,'Auction oluşturulamadı.'));}finally{setSaving(false);}};
  const action=async(id:string,kind:'schedule'|'activate'|'finalize'|'cancel'|'relist')=>{setSaving(true);try{if(kind==='schedule')await adminApi.scheduleAuction(id,{startsAt:localIso(5),endsAt:localIso(65)});if(kind==='activate')await adminApi.activateAuction(id);if(kind==='finalize')await adminApi.finalizeAuction(id);if(kind==='cancel')await adminApi.cancelAuction(id);if(kind==='relist')await adminApi.relistAuction(id,{startsAt:localIso(10),endsAt:localIso(70)});setMessage(`Auction ${kind} işlemi tamamlandı.`);load();}catch(e){setError(getApiErrorMessage(e,'Auction işlemi başarısız.'));}finally{setSaving(false);}};
  return <div><div className="page-header"><div><h1 className="page-title">Açık Artırma Yönetimi</h1><p className="page-subtitle">Create/schedule/activate/finalize/cancel/relist admin endpointleri.</p></div><button className="btn btn-primary" onClick={()=>setShowAdd(true)}><Plus size={16}/> Yeni Auction</button></div>{error&&<div className="error-banner" style={{marginBottom:16}}>{error}</div>}{message&&<div style={{color:'#6ee7b7',marginBottom:16}}>{message}</div>}<div className="data-table-wrapper"><div className="data-table-header"><div className="search-bar" style={{minWidth:300}}><Search size={15}/><input placeholder="Auction ara..." value={search} onChange={e=>setSearch(e.target.value)}/></div></div>{loading?<Empty text="Açık artırmalar yükleniyor..."/>:filtered.length===0?<Empty text="Açık artırma bulunamadı."/>:<table className="data-table"><thead><tr><th>Ürün</th><th>Güncel</th><th>Min Teklif</th><th>Bitiş</th><th>Durum</th><th style={{textAlign:'right'}}>İşlem</th></tr></thead><tbody>{filtered.map(a=><tr key={a.id}><td><strong>{a.productName}</strong><div style={{color:'var(--text-muted)',fontSize:'0.78rem'}}>{a.sku}</div></td><td>{formatMoney(a.currentPrice,a.currency)}</td><td>{formatMoney(a.minimumNextBid,a.currency)}</td><td>{new Date(a.endsAt).toLocaleString('tr-TR')}</td><td><span className="badge badge-purple">{auctionStatusLabel[String(a.status)]??String(a.status)}</span></td><td style={{textAlign:'right'}}><div style={{display:'flex',gap:6,justifyContent:'flex-end',flexWrap:'wrap'}}><button className="btn btn-ghost" disabled={saving} onClick={()=>action(a.id,'schedule')}>Schedule</button><button className="btn btn-ghost" disabled={saving} onClick={()=>action(a.id,'activate')}>Activate</button><button className="btn btn-ghost" disabled={saving} onClick={()=>action(a.id,'finalize')}>Finalize</button><button className="btn btn-ghost" disabled={saving} onClick={()=>action(a.id,'relist')}>Relist</button><button className="btn btn-ghost" disabled={saving} style={{color:'#f87171'}} onClick={()=>action(a.id,'cancel')}>Cancel</button></div></td></tr>)}</tbody></table>}</div>{showAdd&&<div className="modal-overlay" onClick={()=>setShowAdd(false)}><div className="modal-content" onClick={e=>e.stopPropagation()} style={{maxWidth:560}}><Header title="Yeni Auction" close={()=>setShowAdd(false)}/><Select label="ProductListing" value={form.productListingId} onChange={v=>setForm(p=>({...p,productListingId:v}))} options={listings.map(l=>({value:l.id,label:`${l.productName} - ${formatMoney(l.price,l.currency)}`}))}/><Input label="Başlangıç Fiyatı" value={form.startingPrice} onChange={v=>setForm(p=>({...p,startingPrice:v}))}/><Input label="Minimum Artış" value={form.minimumBidIncrement} onChange={v=>setForm(p=>({...p,minimumBidIncrement:v}))}/><Input label="Adet" value={form.quantity} onChange={v=>setForm(p=>({...p,quantity:v}))}/><p style={{color:'var(--text-muted)',fontSize:'0.8rem'}}>Anti-snipe son 5 dakika +5 dakika backend domain kuralıdır. Bid spam/rate limit UI’da yok; teknik borç.</p><div style={{display:'flex',justifyContent:'flex-end',gap:12,marginTop:20}}><button className="btn btn-ghost" onClick={()=>setShowAdd(false)}>İptal</button><button className="btn btn-primary" disabled={saving} onClick={create}><Check size={16}/> Oluştur</button></div></div></div>}</div>;
}
function Empty({text}:{text:string}){return <div style={{padding:'60px 0',textAlign:'center',color:'var(--text-muted)'}}><Gavel size={32} style={{margin:'0 auto 12px',opacity:.3}}/><p>{text}</p></div>}
function Header({title,close}:{title:string;close:()=>void}){return <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:20}}><h2>{title}</h2><button className="btn btn-ghost" style={{padding:4}} onClick={close}><X size={18}/></button></div>}
function Input({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <div className="form-group"><label className="form-label">{label}</label><input className="form-input" value={value} onChange={e=>onChange(e.target.value)}/></div>}
function Select({label,value,onChange,options}:{label:string;value:string;onChange:(v:string)=>void;options:Array<{value:string;label:string}>}){return <div className="form-group"><label className="form-label">{label}</label><select className="form-input" style={{appearance:'auto'}} value={value} onChange={e=>onChange(e.target.value)}><option value="">Seçiniz</option>{options.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select></div>}
